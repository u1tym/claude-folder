{
  "name": "file-version-manager-frontend",
  "version": "1.0.0",
  "description": "Frontend for file versioning system with memo support",
  "scripts": {
    "dev": "python -m http.server 8080",
    "build": "echo 'Static files ready'"
  }
}